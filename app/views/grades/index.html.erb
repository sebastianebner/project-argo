
<div class="grades-container">

	<div class="page-header">
		<h1> Welcome <%=current_user.name %>, please enter your grades here.</h1>
	</div>
	<form action="<%= grades_path %>" method="POST">
		<div class="form-shrinker">
			<div class="row">

				<% @subjects.each do |subject| %>

					<div class="col-md-6">
						<div class="input-group">
							<span class="input-group-addon" id="basic-addon1"> <%= subject.name %> </span>
							<input type="text" class="form-control" placeholder="Enter grade here" name="<%= subject.name %>" >
						</div>
					</div>

				<% end %>

			</div>
		</div>

		<div class="submit-button">
			<input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">	
			<input type="submit" class="btn btn-info">Submit</a></a>
				
		</div>


	</form>
</div>

